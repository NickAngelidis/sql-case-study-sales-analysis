# SQL Case Study #1 – Sales Analysis (MySQL)

This is a beginner-friendly **SQL project** built with **MySQL**, where we analyze a simple sales dataset to answer common business questions.  
The project demonstrates essential SQL techniques such as `SELECT`, `GROUP BY`, `ORDER BY`, `CASE`, subqueries, and basic window functions.

---

## Project Structure

- `sales_data.sql` – Creates the schema `sales_analysis` and inserts sample data.
- `queries.sql` – Contains all queries (basic and advanced).
- `README.md` – Project description and explanations.

---

## 📊 Business Questions Answered

**Basic Level:**
1. What is the total revenue?
2. How many units of each product were sold?
3. What is the average selling price per product?
4. What is the total revenue per month?
5. Which product generated the highest revenue?
6. Which three days had the highest revenue?
7. What is the monthly revenue per product?
8. What percentage of total revenue does each product contribute?

---

## How to Run

1. Open MySQL Workbench (or any MySQL client).
2. Run `sales_data.sql` to create the database and insert sample data.
3. Run `queries.sql` to explore the data and answer the questions.


---

This project is designed for beginners learning SQL and building a portfolio for data analytics roles.
